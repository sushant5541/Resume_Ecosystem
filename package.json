{
  "name": "resume-ecosystem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix backend\" \"npm:dev --prefix frontend\"",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "cross-env": "^10.1.0"
  }
}
